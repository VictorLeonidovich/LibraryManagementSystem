# 📚 Library Management System

Веб-приложение для управления библиотечной системой, построенное на базе Java-фреймворка Spring Boot.

**GitHub Repository:** [VictorLeonidovich/LibraryManagementSystem](https://github.com/VictorLeonidovich/LibraryManagementSystem)

- [x] За основу для последующего расширения возможностей и функциональности взят pet-проект на Spring Boot 2 из курса:
  * Название: Spring boot using Intellij: Build a real world project
  * Год выпуска: 2022
  * Автор: Falsal Memon
  * Производитель: Udemy
  * Сайт производителя: https://www.udemy.com/course/spring-boot-using-intellij-build-a-real-world-project

---

## 🛠 Технологический стек

* **Backend:** Java, Spring Boot 4, Spring Data JPA (Hibernate)
* **Frontend:** HTML5, Thymeleaf
* **Сборка и зависимости:** Maven, MapStruct
* **Базы данных и миграции:** Liquibase
* **Мониторинг и валидация:** Spring Boot Actuator, Jakarta Validation

---

## 🏛 Архитектура проекта

Приложение реализует классическую монолитную архитектуру:
* **UI-слой:** Серверный рендеринг (SSR) веб-страниц с помощью Thymeleaf.
* **API-слой:** Выделенный REST API с DTO-маппингом (MapStruct) и собственным обработчиком исключений.
* **Бизнес-логика:** Сервисный слой на Java для обработки правил библиотеки.
* **Данные:** Модель данных (JPA-сущности) для книг, авторов, категорий и издателей.

---

## 📈 Текущий прогресс и дорожная карта (Roadmap)

### ✅ Что уже сделано
- [x] Выполнена миграция со Spring Boot 2 на актуальную версию Spring Boot 4.0.6.
- [x] Настроены профили окружения для изоляции сред разработки, тестирования и деплоя:
  * `dev` — локальная разработка на быстрой in-memory базе данных H2 с автоматическим заполнением тестовыми данными.
  * `test` — изолированная среда для автоматического прогона тестов на H2 в режиме совместимости с PostgreSQL.
  * `containers` — профиль для запуска интеграционных тестов в Docker-окружении с использованием динамических контейнеров Testcontainers с PostgreSQL.
  * `prod` — промышленный режим работы приложения с подключением к физической СУБД PostgreSQL, валидацией схемы данных и защитой конфиденциальных учетных данных через переменные окружения.
- [x] Подключен и настроен Spring Boot Actuator для мониторинга приложения.
- [x] Реализована валидация входных данных с помощью Jakarta Validation.
- [x] Настроена глобальная обработка ошибок для UI-контроллеров (`@ControllerAdvice` -> `GlobalExceptionHandler`).
- [x] Создан отдельный REST API слой:
    * Внедрены `@RestController` и DTO-маппинг через MapStruct.
    * Настроена изолированная обработка ошибок для API (`@RestControllerAdvice` -> `ApiGlobalExceptionHandler`).
- [x] Реализована бизнес-логика для всех сущностей:
    * Пагинация и сортировка данных.
    * Кастомные методы в репозитории через `@Query`.
    * Управление транзакциями на сервисном уровне.
- [x] Накатывание базовой структуры БД и демо-данных через Liquibase.
    * Конфигурация Liquibase приведена к стандартному виду (исправлена структура каталогов/файлов, упрощены `.properties`, удален кастомный `LiquibaseInitializer`).
- [x] **Безопасность (Security):**
  * Защищен REST API с использованием JWT-токенов.
  * Настроена ролевой модели доступа (`USER`, `ADMIN`).
  * Реализована строгая валидация входных данных при регистрации и аутентификации через `Jakarta Validation`.
  * Настроен централизованный перехват и обработка JWT-исключений (`ExpiredJwtException`, `JwtException`) внутри цепочки фильтров с возвратом кастомных JSON-ответов (400/401).
  * Архитектура API приведена к стандартам REST: эндпоинты авторизации переведены с сырых `Map` на типизированные DTO (`UserRegisterDto`, `UserLoginDto`, `UserResponseDto`).
- [x] **Тестирование (Testing):**
  * Юнит-тесты для сервисов с использованием Mockito.
  * Тестирование веб-слоя (контроллеров) через MockMvc.
  * Тестирование репозиториев (@DataJpaTest -> Testcontainers)
  * Тестирование мапперов
  * Интеграционное тестирование с базой данных через Testcontainers.
  * Настройка плагина JaCoCo, чтобы сгенерировать HTML-отчет и увидеть точный процент покрытия кода на понятных графиках.
- [x] **Документация и DevOps:**
  * Интеграция Swagger/OpenAPI для документирования REST API.
  * Контейнеризация приложения с помощью Docker.
  * Настройка CI/CD пайплайна через GitHub Actions для автоматического запуска Unit (H2) и интеграционных тестов (Testcontainers) при каждом коммите или Pull Request с валидацией покрытия JaCoCo (>70%).

### 🔄 В планах и разработке
- [ ] **Рефакторинг:**
  * [ ] Удаление легаси-кода, общий рефакторинг и исправление замечаний SonarQube.

---

## 🚀 Перспективы дальнейшей разработки (Project Roadmap)

Для вывода системы на уровень полноценного enterprise-решения планируется реализация следующих архитектурных и функциональных задач:

### 1. Качество кода и локализация
* [x] **Документирование исходного кода:** Покрытие всех публичных интерфейсов, реализаций бизнес-логики (Service layer) и доменных моделей полноценными **JavaDoc** комментариями для повышения читаемости и поддерживаемости кода.
* [ ] **Интернационализация (i18n):** Вынос всех жестко закодированных сообщений об ошибках, уведомлений валидации и текстовых элементов интерфейса в отдельные файлы ресурсов (`messages.properties`) для обеспечения мультиязычности.

### 2. Производительность и масштабируемость
* [ ] **Распределенное кэширование (Redis):** Интеграция механизма **Spring Cache с базой данных Redis** в качестве слоя распределенного кэширования для тяжелых и часто запрашиваемых данных (например, списков популярных книг, каталогов), что позволит снизить нагрузку на основную БД и ускорить ответы API.

### 3. Наблюдаемость системы и мониторинг (DevOps)
* [ ] **Метрики и дашборды:** Настройка сборщика **Spring Boot Actuator** совместно с **Micrometer** для экспорта системных метрик приложения, интеграция с **Prometheus** для сбора данных и развертывание **Grafana** для визуализации производительности JVM, потребления памяти и пропускной способности HTTP-запросов в реальном времени.
* [ ] **Централизованный сбор логов (ELK Stack):** Интеграция связки **Elasticsearch, Logstash и Kibana** для агрегации и анализа логов, генерируемых при REST-взаимодействии, что обеспечит быстрый поиск по логам, структурированную аналитику и выявление аномалий.

### 4. Расширенный бизнес-функционал
* [ ] **Экспорт данных и отчетность:** Добавление функционала экспорта данных при просмотре книги, позволяющего пользователям сохранять метаданные, историю или карточки книг в форматы **Excel (XLSX)** и **PDF**.
* [ ] **Интеграция почтового сервиса:** Реализация модуля отправки уведомлений по электронной почте на базе Spring Boot Mail для отправки электронных версий книг, подтверждений заказов или напоминаний о возврате.

### 5. Событийно-ориентированная архитектура (EDA) и симуляция
* [ ] **Симулятор активности пользователей:** Создание изолированного сервиса, который будет имитировать реальное поведение множества конкурентных пользователей, активно отправляя REST-запросы к основному приложению.
  * [ ] **Интеграция брокера сообщений:** Использование **Apache Kafka** в качестве платформы потоковой передачи данных для асинхронной передачи событий активности от симулятора к основной системе, обеспечивая слабую связность (loose coupling) компонентов.

---

## 📌 Документация API и тестирование

После запуска приложения интерактивная документация Swagger UI доступна по адресу:
👉 **http://localhost:8080/swagger-ui.html**

### Как тестировать защищенные эндпоинты в Swagger UI:
1. Раскрой блок **Авторизация** и выбери эндпоинт `/api/auth/login`.
2. Нажми **Try it out**, введи логин/пароль пользователя и выполни запрос.
3. Скопируй строку полученного токена (`token`) из JSON-ответа.
4. Поднимись в самый верх страницы Swagger и нажми зеленую кнопку **Authorize**.
5. Вставь скопированный токен в поле ввода и нажми **Authorize**. Теперь ты можешь полноценно тестировать все защищенные эндпоинты для ролей `USER` и `ADMIN` прямо из браузера!

---

## 📊 Мониторинг и метрики (Spring Boot Actuator)

В проект встроен модуль **Spring Boot Actuator** для мониторинга состояния приложения, метрик JVM и доступности базы данных в реальном времени.

Базовая точка входа:
👉 **http://localhost:8080/actuator**

Поведение системы настроено в зависимости от активного профиля:

### 🛠️ В профиле разработки (dev):
Доступны **все** встроенные эндпоинты управления (`management.endpoints.web.exposure.include=*`).
* **`/actuator/health`** — показывает расширенную информацию о состоянии системы и детальный статус подключения к БД H2 (`show-details=always`).
* **`/actuator/metrics`** — предоставляет полные системные метрики (использование памяти JVM, процессора, счетчики HTTP-запросов). Для просмотра конкретной метрики добавьте её имя в путь, например: `/actuator/metrics/jvm.memory.used`.
* **`/actuator/loggers`** — позволяет просматривать и динамически менять уровни логирования классов прямо на лету.

### 🚀 В продакшн профиле (prod):
В целях безопасности доступ к инфраструктуре строго ограничен (`management.endpoints.web.exposure.include=health,info`).
* Открыты только эндпоинты **`/actuator/health`** и **`/actuator/info`**.
* Отображение внутренней структуры и деталей подключения к PostgreSQL полностью скрыто (`show-details=never`) для предотвращения утечки данных об архитектуре сервера.

---

## 🚀 Быстрый запуск в Docker (Production Mode)

Для развертывания всего стека (Spring Boot + PostgreSQL 16 + автоматические миграции Liquibase) вам понадобится только установленный **Docker Desktop**.

1. **Настройте окружение:**
   Создайте файл `.env` в корневом каталоге проекта и заполните своими данными (шаблон):
   ```text
   DB_NAME=library_db
   DB_USER=postgres
   DB_PASSWORD=your_secure_password
   DB_PORT=5432
   APP_PORT=8080
   JWT_SECRET=your_ultra_long_random_secret_key_here
   JWT_EXPIRATION=36000000
   ```

2. **Запустите контейнеры:**
   Выполните команду в терминале:
   ```bash
   docker-compose up --build -d
   ```

3. **Проверьте статус:**
  * Приложение будет доступно по адресу: `http://localhost:8080`
  * Документация Swagger UI: `http://localhost:8080/swagger-ui.html`
  * Посмотреть логи приложения: `docker-compose logs -f library-app`

---

## 🛠 Статус сборки проекта (CI/CD)

![Java CI/CD Pipeline](https://github.com)

*Бейдж автоматически обновляется при каждом коммите в ветки `main`, `master` или `develop`.*